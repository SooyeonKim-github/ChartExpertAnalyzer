[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "chart-confluence-selector"
version = "0.1.0"
description = "Lecture-grounded chart confluence stock selector"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
